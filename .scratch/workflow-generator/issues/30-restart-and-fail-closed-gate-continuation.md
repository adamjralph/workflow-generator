# 30: Restart and fail closed from a committed Gate pause

**Type:** task
**Status:** accepted by Adam for the bounded offline D2 restart path, 2026-09-25 17:19 AEST. Scoped implementation commit `b53fb492a5568d675749e2649c9e929576672010` pushed and remote read back at that SHA; publication record follows separately.
**Blocked by:** Nothing within this accepted scope. Fork/between-wave Gates, arbitrary mid-step exactly-once side effects, live model/tool effects and person-level authentication remain out of scope.
**Capability:** Completes the accepted bounded P17 restart promise; not arbitrary mid-step exactly-once execution.
**Contract:** Accepted D2 Gate identity contract and ADR 0012.
**Review baseline:** `4dddff4be891923452bdfdc06bc5f3e3340a10fd` is the repository HEAD at ticket publication, not the eventual implementation base. At start, record the accepted ticket-29 source/evidence hash and current working-tree diff; independent reviewers must assess this ticket's frozen delta against that actual base.

**What to build:** After a workflow durably pauses at a Gate, the operator decides through the scoped local CLI, exits, and starts a fresh process. The new process loads the retained immutable version and committed pause from the caller-named store, claims that run exclusively, verifies artifact identity and the decision/event chain, then executes only remaining work. Repeated or concurrent resume cannot repeat a step, charge the Gate again, or silently inherit approval; corruption and ambiguous crash states stop before downstream work. The reference and emitted graph remain independently checkable offline.

**Public test seam:** The ticket-29 authored Route/Gate workflow and frozen registered operations + externally stored artifact/checkpoint/decision/event receipts + fresh process boundary + independent offline approvals and typed cases → final state, canonical event/budget comparison, recovery receipt and conformance report. Expose deterministic crash/write-failure injection at declared commit boundaries; no live model/tool calls or Hermes writes.

## Acceptance criteria

- [x] A real fresh process reconstructs the same admitted executable identity and post-Gate state from retained bytes, with no arbitrary entry or caller-reconstructed state, and completes only the approved remainder. Hand-authored expected state, exact spent/remaining budget and canonical event chain match independently running plain and emitted-graph drivers and offline conformance.
- [x] Define atomic publication/recovery ordering for immutable artifacts, event chain, committed pause/checkpoint and decision; prove with crash injection at each relevant boundary which evidence is committed and which state is indeterminate. A mid-step in-flight crash is never automatically replayed, and this ticket makes no exactly-once claim for arbitrary effects.
- [x] Per-run exclusive continuation claim prevents concurrent or duplicate resumes. Repeated resume adds no step, event or effect; conflicting decision/claim fails closed. Approval is consumed for exactly the original run/Gate/pause and cannot transfer to a later visit or altered version.
- [x] Missing, torn, truncated or corrupt artifact, spec, bundle, binding/config/dependency, checkpoint, decision or event-chain evidence; forged/stale identity; wrong run/Gate/pause; and conflicting/late decisions are rejected before downstream work. Re-derive identities at recovery rather than trusting stored digest fields.
- [x] Audit-write and checkpoint/decision commit failures do not report successful continuation or execute downstream work. Tests distinguish a committed pause eligible for recovery from an ambiguous partial write or in-flight step that requires explicit resolution/new run.
- [x] Independent Standards and Spec reviews inspect frozen final source and evidence, blockers are resolved, and focused/type/full offline validation is read back before proposing P17 acceptance. Record exact baseline/hash and any remaining limitations; no build, commit, push or live call follows from publication of this ticket.

**Out of scope:** Gate inside a Fork or between waves, regeneration, live model/tool side effects, general arbitrary-crash exactly-once replay, person-level authentication, public persistent spec serialization. The separate implementation go was supplied by Adam's `/implement Ticket 30` direction; acceptance, delegation, commit and push remain separate.

## Implementation checkpoint — 2026-09-25

Adam's explicit `/implement Ticket 30` direction supplied the implementation go. A restricted offline restart candidate is in the shared working tree; see [restart contract](../../../docs/ticket-30-restart.md) and `tests/test_gate_restart.py`. It is **not accepted**. It preserves the two unrelated untracked ticket-28 briefs. No live workflow/provider call, agent delegation, commit or push has been made. Final-source full suite, review and status evidence must be read back before proposing acceptance; do not turn a historical full-suite attempt into final-source proof.

**15:31 AEST correction checkpoint:** Adam approved independent reviews; Standards and Spec both requested changes on the original seven-file hash `1a7c33e484dc2676494a00eb6258d55d380fa7cfa8e200adbeaa572c37723357`: (1) hard-coded four-envelope recovery rejected admitted multi-Transform prefixes, and (2) failed decision directory fsync left a consumable signed decision. RED public-seam tests reproduced both (5 failed); patches now accept a semantically validated variable-length prefix and retain a `.pending-*` refusal marker after linked-decision fsync failure. The five RED cases turned green and mypy was clean in 43 files. Additional corruption tests passed 8 after test tightening. **No final-source full suite or independent confirmation of this corrected delta yet.** See [evidence](../../../docs/ticket-30-executor-evidence.md). Adam asked for handoff at this point; pause implementation here.

**16:51 AEST continuation checkpoint:** Two further independent read-only reviews of the corrected eight-file candidate found the failed-publication marker was not checked by an already-open worker or during recovery/CLI races. This is a new blocking finding, not approval. The open-worker failure was reproduced RED (2 tests); the retained worker now serializes decision consumption using the CLI operator-key lock and refuses ambiguity markers. Same-process and overlapping recovery regression cases passed. Final-source Gate-focused **161 passed**, mypy **43 clean** (`v-EPYU9M`); full offline suite has been started but has no read-back exit yet. New source/test/contract hash has **not** been independently confirmed. No ticket/P17 acceptance, commit, push or live workflow/provider call. See [evidence](../../../docs/ticket-30-executor-evidence.md).

**17:19 AEST acceptance:** Adam expressly accepted ticket 30/P17 under the restricted offline D2 contract, after final-source focused **161 passed**, mypy **43 clean**, full suite **2,330 passed / 3 skipped** with exit **0**, and both independent Standards/Spec confirmations of reviewed eight-file hash `157da944ce455091664ceffbc6abaa96f4ff9ca236578ca33d67b43e2431a719`. Acceptance is a decision, not a commit, push, live workflow call, Hermes write, or expansion of the OS-account trust and committed-pause-only restart boundary. This status edit changes the issue's post-review hash, not the reviewed implementation or contract. See [controller evidence](../../../docs/ticket-30-executor-evidence.md).
