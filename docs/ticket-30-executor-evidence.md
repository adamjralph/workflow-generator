# Ticket 30 controller evidence — accepted bounded offline P17

Baseline: `main` HEAD `0f4acb3cb04cfe7017bb8169623e1d179b5fe792` before the uncommitted ticket-30 delta. The accepted ticket-29 110-entry review hash was `0d3c0799bb1e5971820e87fc28b13a106c6c68444716694c24385f3baa9a76ca`; ticket 30 changes its retained worker and is **not** approved by that review. Two unrelated untracked ticket-28 briefs were preserved.

## Public behavior observed

- `tests/test_gate_restart.py` exercises separate Python controller processes, both reference/graph retained workers, explicit local approve/reject, hand-authored state/event/budget results and one-use continuation. `check_gate_restart` compares independent operator decisions against authored expectations and retained audit heads, not just driver agreement. No live provider, Hermes, or arbitrary callable is used.
- A nonblocking `flock` held across validation and worker lifetime rejects concurrent in-process and separate-process claimants. Completed runs cannot reopen or add a step. The CLI and recovery re-derive spec/bundle identities and committed pause; the worker verifies the retained prefix/proof and decision before successor dispatch.
- Injection covers worker publication sequences 0–7 at `before_file`, `after_file`, `after_link`, `after_dirsync`, `after_unlink`, plus a forced local decision-publication failure. Ambiguous partial writes and in-flight steps refuse replay; a fully directory-synced pause whose acknowledgement was lost can still be inspected, decided and resumed. Corrupt/missing event, decision, lock, metadata and bundle cases refuse before downstream work. This does **not** establish arbitrary external-effect exactly-once execution or resistance to malicious same-UID mutation.

## Validation read-back

- New restart crash subset: **41 passed / 15 deselected** at `/home/hermes/workflow-validation-scratch/v-M2mQmW/` (exit 0); mypy **43 source files clean** in the same command. A focused decision-publication failure passed at `v-Ms94XP`. An earlier Gate-focused run found a `BrokenPipeError` while closing a crashed worker; the teardown was corrected, and the four affected cases passed at `v-609t6F` with mypy clean. The earlier 74/75 attempt is **not** green evidence.
- Final-source full offline regression: `/home/hermes/workflow-validation-scratch/v-vHlLql/full.log` and `exit-code.txt` read back: **2,322 passed / 3 skipped in 996.70s**, exit **0**. Skips: two opt-in live Jev cases and optional real Hermes plugin-loader check. The runner recorded `WORKFLOW_VALIDATION_DIR=/home/hermes/workflow-validation-scratch/v-DFSB3H`; the wrapper used a fresh short child temp dir `/home/hermes/workflow-validation-scratch/v-QNKUsm` for Chromium. Earlier full attempts were intentionally terminated after final source/test changes (exit 143); do not count them.
- `git diff --check` passed. Source/test/contract/issue manifest hash `1a7c33e484dc2676494a00eb6258d55d380fa7cfa8e200adbeaa572c37723357`, defined as SHA-256 of explicit `path + NUL + SHA-256(file bytes)` records in this listed order: `agent_lab/gate_cli.py`, `agent_lab/gate_conformance.py`, `agent_lab/gate_runtime.py`, `agent_lab/gate_worker.py`, `tests/test_gate_restart.py`, `docs/ticket-30-restart.md`, `.scratch/workflow-generator/issues/30-restart-and-fail-closed-gate-continuation.md`. This is a checkpoint, not an independently approved hash. Status-only edits to issue/doc paths after this packet will change it.

## Review and acceptance boundary

A request for specific authorization of two independent read-only Standards/Spec subagent reviews timed out with no response. **No delegation or independent review was launched.** Manual controller inspection found and corrected the lost-acknowledgement directory-fsync ordering and crashed-worker close path before final validation, but does not substitute for the required independent reviews. Ticket 30/P17 remains an **unaccepted candidate**. Ask Adam for that review go, freeze the exact reviewed delta/hash, resolve blockers and revalidate, then ask Adam separately for acceptance. No commit, push, live workflow call or publication was made.

**2026-09-25 15:19 AEST continuation:** Adam subsequently expressly approved the two read-only independent reviews. Standards and Spec were dispatched together as `deleg_f39c755d` against unchanged checkpoint hash `1a7c33e484dc2676494a00eb6258d55d380fa7cfa8e200adbeaa572c37723357`; findings are **pending**, not approved. Controller must inspect both findings, verify source hash remains unchanged, and address/revalidate blockers. No commit, push or live workflow call follows from review authorization.

**15:31 AEST correction checkpoint (handoff requested):** Both reviews returned changes requested. Standards found the hard-coded four-envelope pause; Spec independently found it plus a decision publication failure after link/before directory fsync that left an apparently consumable signed decision. Reviewers reported `openai-codex/gpt-6-astra` session identity, not provider-wire attestation. Standards' `graft map` auto-refreshed its context index despite read-only instructions; Git status showed no source changes from that. Controller reproduced both through five RED public-seam cases (`/home/hermes/workflow-validation-scratch/v-hcv9FZ/`), then patched `gate_cli.py` and the retained `gate_worker.py` to validate variable-length semantic prefixes; patched `gate_store.py` to retain `.pending-*` as a refusal marker if the linked decision's directory fsync fails. Five targeted cases passed and mypy was clean in 43 source files (`v-1HJlqL/`). Eight corruption tests passed after tightening owner-only fixture modes and typed refusal checks (`v-NYrhPd/`). One intermediate corruption-test run had six passes/two failures because missing-lock/missing-version raised raw `FileNotFoundError`; `gate_runtime.py` now maps those to `GateIdentityError`, and all eight then passed. Older full-suite **2,322/3** at `v-vHlLql` is for the **pre-review source** and does not validate these corrections.

Current eight-file checkpoint SHA-256 `2be81fe5a2f0089ee3c0ecbd0ebf3a3ad04671c786d011e63238a415d51168b8` is calculated as `path + NUL + SHA-256(file bytes)` in this order: `agent_lab/gate_cli.py`, `gate_conformance.py`, `gate_runtime.py`, `gate_store.py`, `gate_worker.py`, `tests/test_gate_restart.py`, `docs/ticket-30-restart.md`, issue 30. This checkpoint has **not** had full focused Gate regression, final-source full suite, or independent confirmation; no acceptance claim. Adam asked to hand off now. No commit, push, live workflow call, profile modification or further delegation.

**16:42 AEST continuation — corrected review exposed a further blocker:** The prior eight-file manifest was replaced by a documentation clarification; the explicitly defined `path + NUL + lowercase SHA-256(file bytes) hex` eight-file hash `8118c0c8a79f295c1f75854540bce56803180202533026e7af32754473480ccf` was independently confirmed by both new read-only reviewers (`deleg_8982df27`, session configured `openai-codex/gpt-6-astra`, no wire attestation). They found that an already-open worker, or a recovery that inspected before concurrent failed publication, could consume a linked but unsynced decision. The controller reproduced the open-worker path RED (2 failures) at `v-c9ohJo`, then changed the retained worker to take the CLI's operator-key lock and refuse `.pending-*` before decision consumption. Two open-worker cases plus the existing failure case passed at `v-qY754B`, mypy clean in 43 files; an interleaving test passed at `v-4Oe2uK`. The pre-fix full run at `v-sj0Q1H` was killed after review findings and is **not** a pass. The pre-fix corrected candidate had 158 focused Gate tests pass and mypy 43 clean at `v-bzOsNE`. A new final-source focused/type run is in progress; final full suite and independent confirmation of the new hash remain pending. Ticket 30/P17 remains unaccepted; no commit, push or live provider/workflow call.

**17:09 AEST final-source verification:** Focused Gate **161 passed** in 571.39s and mypy **43 source files clean** at `/home/hermes/workflow-validation-scratch/v-EPYU9M/`, both exit 0. Final-source full offline suite at `/home/hermes/workflow-validation-scratch/v-RMe49p/`: `full.log` read back **2,330 passed / 3 skipped in 1063.58s**, `exit-code.txt` read back **0**. Two skips require opt-in live Jev calls; one is optional real Hermes plugin-loader integration. Fresh child browser TMPDIR was `/home/hermes/workflow-validation-scratch/v-U6FvVq`. Independent Standards and Spec re-reviews `deleg_cade2deb` both returned **PASS/no blockers** on eight-file SHA-256 `157da944ce455091664ceffbc6abaa96f4ff9ca236578ca33d67b43e2431a719` (path UTF-8 + NUL + lowercase SHA-256(file bytes) hex, paths listed above). The controller recalculated it unchanged after full regression; `git diff --check` passed. Reviewers' session-configured route was `openai-codex/gpt-6-astra`, not wire-attested. Spec noted one **nonblocking** gap: no forced consumption while CLI still holds its lock; this is not an approval of person-level authentication or general exactly-once side effects. Ticket 30/P17 is **technically verified, not accepted by Adam**. No commit, push, live model/tool call, profile edit, or ticket closure. Ask Adam for acceptance under the bounded D2 contract; commit/push separately.

**17:19 AEST acceptance decision:** Adam expressly accepted ticket 30/P17 under the bounded offline D2 contract. This decision closes the acceptance gate only. The issue's status/checklist/acceptance note and roadmap/map/CURRENT/HANDOFF reconciliation follow the verified pre-acceptance review; the issue's new bytes change the eight-file manifest, while the reviewed implementation, tests and restart contract remain unchanged. No new test run is claimed for documentation-only status edits. No commit, push, live provider/tool workflow, or Astra-profile edit occurred. The two unrelated untracked ticket-28 briefs and private validation roots remain untouched. Repository publication is Adam's separate decision.
