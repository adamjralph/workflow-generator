# 29: Pause, decide and continue one Route Gate

**Type:** task
**Status:** accepted by Adam (2026-09-25 13:34 AEST)
**Blocked by:** None.
**Acceptance boundary:** Adam expressly accepted the independently reviewed, bounded **same-process** offline Route → Gate → local OS-account decision → continuation path after final-source validation. This does not prove fresh-process recovery, complete P17, person-level authentication or arbitrary Python-binding safety. Acceptance does not authorize ticket 30 implementation, live calls, a commit or a push. The controller disclosed that independent read-only subagent reviews were launched without the handoff's separately required delegation permission; that boundary error is not retroactively authorized.
**Accepted evidence:** Final-source focused Gate suite **96 passed**, mypy **43 source files clean**, full offline suite **2,265 passed / 3 skipped**, exit **0** (`/home/hermes/workflow-validation-scratch/v-Ng7Rey/full.log` and `exit-code.txt`). Independent Standards and Spec both passed the corrected implementation. Pre-acceptance frozen review manifest (110 entries) SHA-256 `0d3c0799bb1e5971820e87fc28b13a106c6c68444716694c24385f3baa9a76ca`; source/test/contract (109 entries) `a9e65a347c9903d0092a931759762f82793afcf6af3d375cd7e79572a96cb266`. All entries matched before this acceptance-status edit, which changes the issue's hash but not the reviewed implementation. See `docs/ticket-29-executor-evidence.md` and `HANDOFF.md`.
**Authorization boundary:** Begin ticket 29 after the handoff, in bounded offline stages. This does not authorize live provider/tool calls, independent delegation, commit or push; ask separately before those actions.
**Capability:** First P17 vertical slice; completion does **not** complete P17 or establish restart recovery.
**Contract:** Accepted D2 Gate identity contract and ADR 0012. No public persistent spec format is selected.
**Review baseline:** `4dddff4be891923452bdfdc06bc5f3e3340a10fd` is the repository HEAD at ticket publication. The accepted D2 contract, ADR and planning documents are uncommitted working-tree additions/edits; reviewers must inspect their current content and freeze the exact implementation/document set before reviewing. This SHA alone does not contain D2.

**What to build:** An operator authors a minimal non-parallel Route workflow, emits a frozen executable version to a caller-named store outside Hermes, runs it to one Gate, and uses an owner-only local OS-account CLI to approve or reject that exact pause. Approval continues only the approved route without repeating completed work or charging the Gate twice; rejection ends the run. The plain reference and emitted graph run independently, and offline conformance checks the *actual emitted artifact* against hand-authored expected state, canonical events and budget. This is a same-process demonstration; cross-process recovery is ticket 30.

**Public test seam:** Authored spec + registered frozen operation set + emitted version in a caller-named store + explicit run ID + independent offline decision fixtures for each driver + typed cases → reference result, emitted-graph result, conformance report, and readable identity/checkpoint/decision/event receipts. A separate local CLI action records a real operator decision; fixture decisions cannot authorize a real run. Use a temporary external store and no live model, tool side effect or Hermes write.

## Acceptance criteria

- [x] Hand-authored Route → Gate → Route cases demonstrate pending pause, explicit rejection, approved continuation and short/exact run-wide budget. One Gate visit costs exactly one step; submission and continuation cost none. No successor runs while pending, after rejection, or after invalid decision. Both independent drivers and conformance reproduce expected state, canonical events and spend.
- [x] The versioned private canonical spec encoding includes execution-relevant fields and rejects unknown, malformed or noncanonical forms. Store exact immutable bytes, not an asserted digest; re-derive identity on Gate and continuation. A changed spec—even at the same path—cannot inherit an approval. This is not a public import/export format.
- [x] The retained bundle manifest and referenced executable artifacts bind target/emitter version, spec digest, graph configuration, state schema and registered operation code/config/dependency closure. Admit only the accepted small deterministic, no-external-side-effect registry; refuse arbitrary closures, mutable globals, dynamic/unpinned dependencies or unverifiable behavior before work. An import name, function source alone, object identity or caller digest is insufficient.
- [x] A local owner-only CLI derives OS-account identity itself, displays/re-derives both identities and paused run/Gate/attempt, and records one durable scoped approval or rejection. Same-UID agents remain trusted; do not claim human-person authentication or expose remote/general agent approval. Identical resubmission is idempotent; conflicting, stale, wrong-run/Gate/pause/version, forged and late decisions fail closed before downstream work. Existing business-draft approval is not Gate authority.
- [x] Persist a typed versioned post-Gate checkpoint and continuous audit events with exact state, run/Gate/pause, used/remaining budget and both identities. Define and test event/decision/checkpoint write ordering for this same-process path. An audit-write failure prevents successful continuation or downstream execution; malformed/altered identities and evidence fail closed.
- [x] Review with independent Standards and Spec assessments of the frozen implementation and test evidence, resolve blockers, and run focused/type/full offline validation. Publish exact test results and unchanged review baseline/hash; no acceptance claim from driver agreement alone.

**Out of scope:** Fresh-process resume and arbitrary-crash recovery (ticket 30), Fork or Gate-between-waves, regeneration, arbitrary Python bindings, live provider/tool effects, Hermes writes, public spec serialization, commit or push. The requested handoff comes before implementation; preserve all sibling work.
